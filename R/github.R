
gh_fork <- function(source_url, dest_org) {
  return(dest_url)
}

gh_push_branch <- function(local_path, repo_url, branch) {

}

generate_gh_pr_url <- function(source_repo, dest_repo, source_branch, dest_branch = "main") {
}

extract_gh_reponame_from_url <- function(gh_url){
}

gh_open_pr <- function(description, source_repo, dest_repo, source_branch, dest_branch = "main") {
}